// Step 1: set footer year
// document.getElementById("year").textContent = ...

// Step 2: set footer last modified
// document.getElementById("lastModified").textContent = document.lastModified;

// Step 3: hamburger toggle
// - select the menu button and the nav ul
// - add a 'click' event listener to the button
// - inside it: toggle a class (e.g. "hidden" or "show") on the nav ul
// - also toggle the button's text/icon between ☰ and ✕